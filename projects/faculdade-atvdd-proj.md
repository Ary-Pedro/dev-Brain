---
type: project
project: faculdade-atvdd
repo_path: ~/devSpace/projeto/faculdade_ATVDD_INTEGRADORA/atvdd-integradora-vii
scope: faculdade-atvdd
stack: [typescript, vite, react]
brain_policy: dev-brain
write_policy: dev-brain-only
domain_brain: (nenhum)
status: draft
token_weight: baixo
graph_nodes: 0
graph_edges: 0
source: human
audience: ai
confidence: low
last_verified: 2026-06-19
token_policy: summary-first
tags: [project, faculdade, typescript-next]
---

# faculdade-atvdd — card vivo

> Camada operacional p/ o agente decidir contexto. Estrutura (inferida) no mapa [[faculdade-atvdd]].

<!-- @generated:start -->
## Objetivo

Atividade Integradora VII (faculdade). Projeto **independente**, **acadêmico e descartável** — coisas criadas pra depois apagar. Stack inferida (Lovable/Vite + TypeScript + provável React, deploy Cloudflare Workers). **Escopo pequeno, foco baixo por tarefa.** Sem domínio MailerWeb.

> ⚠️ **Recon 2026-06-17: repo vazio no disco** — só `node_modules/` (43 symlinks), zero fonte, sem `package.json`/README. Stack é hipótese dos binários. Antes de codar: restaurar o repo e refazer o mapa a partir do `package.json` real.

## Como consultar

1. **Domínio** → não há; usar **somente dev-Brain**. NUNCA citar MailerWeb (anti-alucinação). `domain_brain=(nenhum)`.
2. **Padrão** (estilo, naming, erros, segurança, testes) → `know/` por stack: [[typescript-next]] (rótulo TS/web; **não é Next.js** — é Vite).
3. **Estrutura** → mapa [[faculdade-atvdd]] (expectativa de scaffold; validar após restaurar).

## Peso p/ IA

`token_weight=baixo` → projeto pequeno/descartável. Resumo + ~2 notas bastam. Não investir contexto pesado; dev-Brain entra só se a tarefa pedir padrão real. Tratar como throwaway até virar algo sério.

## Próxima ação

Restaurar o fonte (`git clone`/`checkout`) e rodar `npm install` antes de qualquer coisa — o `node_modules` atual está incompleto e não há entrypoint no disco.
<!-- @generated:end -->

<!-- @user:start -->
(observações manuais do Pedro)
<!-- @user:end -->

---
Relacionados: [[faculdade-atvdd]] (mapa) · [[routing]].
